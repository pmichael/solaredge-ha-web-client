"""The SolarEdge Web Client integration."""


async def async_setup_entry(_hass: object, _entry: object) -> bool:
    """Set up SolarEdge Web Client from a config entry.

    Real setup is implemented later. This stub exists so config-flow tests can
    patch the hook after CREATE_ENTRY.
    """
    return True
